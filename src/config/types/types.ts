export type navItemType = {
    _id: string;
    url: string;
    title: string;
    icon?: React.ReactNode;
}

export type summaryItemType = {
    _id: string;
    count: string;
    title: string;
}